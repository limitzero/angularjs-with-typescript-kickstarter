The resources folder holds the definitions of how to access external resources (i.e. REST-ful resource via an API or DB resource via its API). Controllers should only talk to services and not resources...